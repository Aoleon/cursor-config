# Configuration Nhost Self-Hosted Local
# Copier ce fichier vers .env.local et remplir les valeurs

# ========================================
# BASE DE DONNÉES - NHOST POSTGRESQL
# ========================================
# Pour utiliser Nhost PostgreSQL local
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nhost

# Alternative: Pour continuer avec Neon DB en développement
# DATABASE_URL=postgresql://user:password@neon-host/database?sslmode=require

# ========================================
# CONFIGURATION DOCKER NHOST
# ========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=nhost

# Hasura GraphQL (non utilisé mais requis par Nhost)
HASURA_GRAPHQL_ADMIN_SECRET=nhost-admin-secret

# Nhost Auth (désactivé car nous utilisons Microsoft OAuth)
AUTH_HOST=localhost
AUTH_SERVER_URL=http://localhost:1338

# Nhost Storage (MinIO)
S3_REGION=us-east-1
S3_ENDPOINT=http://localhost:9000
S3_BUCKET=nhost
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin

# ========================================
# ENVIRONNEMENT APPLICATION
# ========================================
NODE_ENV=development
PORT=4000

# ========================================
# AUTHENTIFICATION MICROSOFT (Conservée)
# ========================================
AZURE_TENANT_ID=your-azure-tenant-id
AZURE_CLIENT_ID=your-azure-client-id
AZURE_CLIENT_SECRET=your-azure-client-secret

# ========================================
# REDIS (Optionnel pour cache)
# ========================================
REDIS_URL=redis://localhost:6379

# ========================================
# AUTRES SERVICES
# ========================================
# OneDrive Sync (si utilisé)
ONEDRIVE_SYNC_ENABLED=false

# Batigest (si utilisé)
BATIGEST_SIMULATED=true

